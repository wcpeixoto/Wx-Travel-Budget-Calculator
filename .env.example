# Optional direct Amadeus browser calls (best used via backend proxy in production)
VITE_AMADEUS_CLIENT_ID=
VITE_AMADEUS_CLIENT_SECRET=

# Optional proxy endpoint for safer server-side API calls (recommended)
# Example: http://localhost:8787
VITE_TRAVEL_PROXY_BASE_URL=
